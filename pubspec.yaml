name: clash_of_clans_clan_helper
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.4 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  aws_client: ^0.4.2
  http: ^1.2.0
  provider: ^6.1.1
  langchain: ^0.3.3
  langchain_openai: ^0.3.3
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - config/
    - assets/
  fonts:
    - family: CustomClashOfClans
      fonts:
        - asset: assets/Supercell-Magic-Regular.ttf
