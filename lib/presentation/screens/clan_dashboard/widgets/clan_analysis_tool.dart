import 'package:flutter/material.dart';

class ClanAnalysisTool extends StatelessWidget {
  const ClanAnalysisTool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
      child: const Text('ClanAnalysisTool'),
    );
  }
}
