import 'package:flutter/material.dart';

class ClanWarHistoryCard extends StatelessWidget {
  const ClanWarHistoryCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('ClanWarHistoryCard');
  }
}
